name: CPU Overall Load Alert
type: metric_aggregation

es_host: 172.25.13.124 
es_port: 9200
index: metricbeat-*

metric_agg_key: system.load.norm.5
metric_agg_type: max

query_key: beat.hostname
doc_type: doc

bucket_interval:
  seconds: 5

sync_bucket_interval: true

max_threshold: 0.7

filter:
- term:
    metricset.name: load

alert:
- slack
slack:
slack_webhook_url: "https://hooks.slack.com/services/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"
slack_channel_override: "#marriott-aws-devops"
slack_username_override: "@Elastalert Admin"

#realert:
#minutes: 10

